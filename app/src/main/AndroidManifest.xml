<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="linkubank.linkubank">
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".budgetStartPage" />
        <activity android:name=".linkProfessionals" />
        <activity android:name=".budgetCreateOption" />
        <activity android:name=".budgetStartSelect" />
        <activity android:name=".incomeSelection" />
        <activity android:name=".expenseSelection" />
        <activity android:name=".employerSelection" />
        <activity android:name=".childSupportAmount" />
        <activity android:name=".scrollingIncomeInputPage" />
        <activity android:name=".scrollingExpenseInputPage" />
        <activity android:name=".reportPage"></activity>
    </application>

</manifest>